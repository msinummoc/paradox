country_decisions = {
	# 一系列动态决议

	# 包括若干决议，按照效果分为几个等级
	# 总的逻辑是这样：
	# 1.为了增加能力，就会增加腐败，为了降低腐败，就要花钱（用决议做可能比让ai自己操作更靠谱）
	# 2.通过印钞可以加钱，但同时加通胀；通胀会根据贸易区传播
	# 3.改变政策会获得一个持续10年或者更久的稳定度提升的debuff，也就是说国家倾向于不稳定
	# 4.战争动员可以获得很大的军事提升（通过职业度来实现），代价是厌战度以及国内的稳定 不喜欢打仗的ai（比如说大明）应该缩减军事开支以保持稳定
	# 5.战争政策有进攻和防御两种，后者增加很多主场优势
	# 6.可以以维护费和叛乱度为代价降低自治，也可以反过来
	# 7.

	SM2_open_decisions_group_decision = {
		major = yes
		potential = {
			ai = no
		}
		provinces_to_highlight = {
		}
		allow = {
			always = yes
		}
		effect = {
			set_country_flag = SM2_player_open_decisions_group_flag
		}
		ai_will_do = {
			factor = 0
		}
		ai_importance = 0
	}

	SM2_manage_policies_decision = {
		major = yes
		potential = {
			ai = no
			has_country_flag = SM2_player_open_decisions_group_flag
		}
		allow = { always = yes }
		effect = {
			country_event = { id = SM2_ai_policies.10 }
		}
		ai_will_do = { factor = 0 }
		ai_importance = 0
	}

	restore_chinese_empire = {
		major = yes
		potential = {
			is_subject = no
			#government = monarchy
			num_of_owned_provinces_with = {
				superregion = china_superregion
				value = 50
			}
			NOT = {has_reform = celestial_empire}
			OR = {
				culture_group = east_asian
				religion = confucianism
				religion = vajrayana
				religion = buddhism
				religion = mahayana
				religion = shinto
				religion = animism
			}
		}
		allow = {
			NOT = {has_country_modifier = lost_mandate_of_heaven}
			num_of_owned_provinces_with = {
				superregion = china_superregion
				value = 50
			}
			total_development = event_target:emperor_of_china
			OR = {
				culture_group = east_asian
				religion = confucianism
				religion = vajrayana
				religion = buddhism
				religion = mahayana
				religion = shinto
				religion = animism
				tag = YUA
				tag = MSA
				tag = MGE
				tag = MCH
				tag = QNG
				tag = TIB
				tag = MSA
				has_reform = chinese_warlord
			}
			has_any_disaster = no
			OR = {
				owns= 1816
				owns= 1821
				owns= 667
			}
		}
		effect = {
			hidden_effect = { dismantle_empire_of_china = yes }
			dismantle_empire_of_china = no
			clr_global_flag = empire_of_china_dismantled
			change_technology_group = chinese
			change_unit_type = chinese
			set_country_flag = chinese
			if = {
				limit = { NOT = { government = republic } }
				change_government = monarchy
				set_emperor_of_china = ROOT
				add_government_reform = celestial_empire
				set_mandate = 60
				set_meritocracy = 100
			}
			add_prestige = 50
			add_stability = 1
		}
		ai_will_do = {
			factor = 400 #Default
		}
		ai_importance = 1000
	}
	


}