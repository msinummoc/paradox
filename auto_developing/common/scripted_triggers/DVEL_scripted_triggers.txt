DVEL_is_right_culture = {
	OR = {
		has_owner_culture = yes
		AND = {
			has_owner_accepted_culture = yes
			NOT = { has_global_flag = DVEL_spread_to_accepted_flag }
		}
		AND = {
			# has_cultural_union = yes
			culture_group = owner
			NOT = { has_global_flag = DVEL_spread_to_same_group_flag }
		}
		# owner = { has_assimilated_culture = PREV }
		# owner = { has_assimilated_culture_group = PREV }
	}
}

DVEL_is_right_government = {
	# OR = {
	# 	government = monarchy
	# 	government = theocracy
	# 	government = republic
	# }
	NOT = {
		OR = {
			# is_nomad = yes
			# uses_horde_unity = yes
			is_migratory_tribe = yes
			can_migrate = yes
		}
	}
}
